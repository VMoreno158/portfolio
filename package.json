{
    "name": "portfolio",
    "version": "1.0.0",
    "scripts": {
        "install:all": "cd back && npm install && cd ../front && npm install",
        "start:backend": "cd back && npm run deploy:api",
        "start:frontend": "cd front && npm start",
        "dev": "concurrently \"npm run start:frontend\" \"npm run start:backend\""
    },
    "devDependencies": {
        "concurrently": "^9.2.1"
    }
}